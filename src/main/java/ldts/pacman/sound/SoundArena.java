package ldts.pacman.sound;

import ldts.pacman.model.game.arena.Arena;

public class SoundArena extends SoundObserver<Arena>{
    public SoundArena(Arena model) {super(model);}
    @Override
    public void onSoundEvent() {
        if(getModel().getPacman().)
    }
}
